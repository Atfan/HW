spring:
  datasource:
      url: jdbc:postgresql://localhost:5436/Ticket-db
      username: sa
      password: admin
      driverClassName: org.postgresql.Driver

  flyway:
      enabled: true
      user: sa
      password: admin
      schemas: public
      url: jdbc:postgresql://localhost:5436/Ticket-db
